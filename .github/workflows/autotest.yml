name: autotest

on: [push, pull_request, workflow_dispatch]

jobs:
  autotest:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/test

